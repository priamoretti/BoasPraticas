using System;
using System.Collections.Generic;
using System.Text;

namespace BoasPraticasProgramacao
{
    class CalculadoraPagamento
    {
    /*    float ResultadoFinal = 0;
        
        public void CalcularPagamentoFuncionario(ParametrosCalculadoraPagamento Recebedor)
        {
                ValidarRecebedor(Recebedor);
                ResultadoFinal = RetornarSalarioCalculado(Recebedor);            
        }

        private void  ValidarRecebedor(ParametrosCalculadoraPagamento Recebedor) {
            if (eInvalidoRecebedor(Recebedor))
            {
                throw new Exception("Recebedor inválido;");
            }
        }

        private bool eInvalidoRecebedor(ParametrosCalculadoraPagamento Recebedor)
        {
            return (Recebedor.Nome.Equals("") || Recebedor.Idade.Equals(0));
        }

        private float RetornarSalarioCalculado(ParametrosCalculadoraPagamento Recebedor)
        {

            float ValorMinimo = 1000;
            bool isSalarioJunior = Recebedor.Salario < ValorMinimo;
            bool isSalarioSenior = Recebedor.Salario >= ValorMinimo;
            float aCrescimoJunior = 0.1f;
            float aCrescimoSenior = 0.2f;
            float ValorSalario = 0;

            if (isSalarioJunior)
            {
                ValorSalario = CalcularSalario(aCrescimoJunior, Recebedor.Salario);
            }

            if (isSalarioSenior)
            {
                ValorSalario = CalcularSalario(aCrescimoSenior, Recebedor.Salario);
            }

            return ValorSalario;
        }

        public float CalcularSalario(float Acrescimo, float Salario)
        {
            return (Salario * Acrescimo) + Salario;
        }
    */
    }

    


}

